viewMember=SELECT * FROM (SELECT ROWNUM AS RNUM, M.* FROM (SELECT * FROM MEMBER)M) WHERE RNUM BETWEEN ? AND ?
selectMemberCount=SELECT COUNT(*) FROM MEMBER

selectMembersearchCount=SELECT COUNT(*) FROM MEMBER WHERE #type LIKE ?
searchType=SELECT * FROM (SELECT ROWNUM AS RNUM, M.* FROM (SELECT * FROM MEMBER WHERE #type LIKE ? )M) WHERE RNUM BETWEEN ? AND ?

TEST=SELECT * FROM MEMBER WHERE USERID LIKE ?